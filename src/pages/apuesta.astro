---
import Boton from "../components/Boton.astro"
import Layout from "../layouts/Layout.astro"
import FormLabelRequired from "../components/Apuestalabel.astro"
---

<Layout title="Nueva Apuesta">
    <Boton href="menu" title="regresar" color="verde" />
    <form action="/submit-bet" method="post" class="form-apuesta">
        <FormLabelRequired tipo="text" id="Deporte" nombre="EleccionDeporte" descripcion="Deporte"/>
        <FormLabelRequired tipo="text" id="Liga" nombre="Eleccionliga" descripcion="Liga"/>
        <FormLabelRequired tipo="date" id="Fecha" nombre="FechaApuesta" descripcion="Fecha de la Apuesta"/>
        <FormLabelRequired tipo="text" id="NombreApuesta" nombre="NombreDeApuesta" descripcion="Nombre de la Apuesta"/>
        <FormLabelRequired tipo="text" id="TipoApuesta" nombre="TipoDeLaApuesta" descripcion="Tipo de la Apuesta"/>
        <FormLabelRequired tipo="text" id="Momio" nombre="Momio" descripcion="Momio"/>
        <FormLabelRequired tipo="text" id="Cantidad" nombre="Cantidad" descripcion="Cantidad"/>
        
        <label for="resultado">Resultado:</label>
        <div class="checkbox">
            <input type="checkbox" id="ganada" name="resultado" value="ganada">
            <label for="ganada">Ganada</label>
            <input type="checkbox" id="perdida" name="resultado" value="perdida">
            <label for="perdida">Perdida</label>
            <input type="checkbox" id="push" name="resultado" value="push">
            <label for="push">Perdida</label>
        </div>
        <button type="submit" class="submit-button">Guardar Apuesta</button>
    </form>

    <label for="deportes">Escoge un deporte:</label>
    <select id="deportes"></select>
    <Boton href="menu" title="regresar" color="verde" />
</Layout>

<style>
.form-apuesta {
    display: flex;
    flex-direction: column;
    width: 50%;
    margin: 0 auto;
    border-radius: 8px;
    background-color: var(--bg-gray);
    padding: 30px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
}

.checkbox-group {
    display: flex;
    gap: 1rem;
    align-items: center;
}

.checkbox-group input[type="checkbox"] {
    margin-right: 0.5rem;
}

.submit-button {
    background-color: var(--primary-green);
    color: white;
    padding: 0.75rem;
    border: none;
    border-radius: 4px;
    font-size: 1rem;
    cursor: pointer;
}

.submit-button:hover {
    background-color: var(--green-dark);
}
</style>

<script>
document.addEventListener('DOMContentLoaded', async () => {
    try {
        const response = await fetch('http://localhost:4322/deportes', {
            credentials: 'include' // Importante para enviar cookies con la solicitud
        });
        if (response.ok) {
            const deportes = await response.json();
            const select = document.getElementById('deportes');
            if (select) {
                deportes.forEach(deporte => {
                    const option = document.createElement('option');
                    option.value = deporte.nombre;
                    option.textContent = deporte.nombre;
                    select.appendChild(option);
                });
            }
        } else {
            console.error('Error al obtener deportes:', response.statusText);
        }
    } catch (e) {
        console.error('Error al obtener deportes:', e);
    }
});
</script>
<script>
    
</script>
